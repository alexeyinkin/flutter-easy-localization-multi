name: my_package
version: 0.1.0

environment:
  sdk: ^3.1.3
  flutter: '>=2.0.0'

dependencies:
  easy_localization: ^3.0.7
  flutter: { sdk: flutter }

dev_dependencies:
  total_lints: ^3.4.0

flutter:
  assets:
    - assets/translations/en.yaml
