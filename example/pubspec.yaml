name: example
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  easy_localization: ^3.0.7
  easy_localization_multi: { path: ../ }
  easy_localization_yaml: ^0.1.1
  flutter: { sdk: flutter }
  my_package: { path: my_package }

dev_dependencies:
  total_lints: ^3.4.0

flutter:
  assets:
    - assets/translations/en.yaml
